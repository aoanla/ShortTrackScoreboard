This is a Short Track Scoreboard, designed for native rendering efficiency (written in OpenGL so it doesn't need a browser), and targetting secure delegation of functions (so you can authorise a device to just be allowed to perform a single task).
